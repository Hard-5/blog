# 部署到公网：让别人也能打开你的博客

> ## 你的专属地址（用户名 `Hard-5`，仓库名 `blog`）
>
> | 步骤 | 直接点这个链接 |
> | --- | --- |
> | 1. 新建仓库 | <https://github.com/new> |
> | 2. 上传文件 | <https://github.com/Hard-5/blog/upload/main> |
> | 3. 开启 Pages | <https://github.com/Hard-5/blog/settings/pages> |
> | 4. **你的博客网址** | <https://hard-5.github.io/blog/> |
>
> ⚠️ 仓库名必须正好是 `blog`，第 2、3、4 条才成立。想起别的名字的话告诉我，我把配置改过来。
>
> `site.json` 里的网址已经填好了，每次构建会自动生成 `feed.xml`（RSS 订阅源）。

## ✅ 已经部署完成了

**你的博客已经上线：<https://hard-5.github.io/blog/>**

仓库地址：<https://github.com/Hard-5/blog>

以后更新文章**不需要**再做下面那些网页操作，只要两条命令：

```bash
cd E:\code\blog
npm run build      # 编译 Markdown
npm run deploy     # 一行发布上线
```

`npm run deploy` 会自动推送、等 GitHub 构建完、然后真的访问一次线上网址确认成功（所以这台机器**不用装 git**）。

下面这些手工步骤请当作**备用方案**：换电脑了、或者命令行的凭据失效了，再照做。

---

目标：拿到一个 `https://xxx.netlify.app` 或 `https://用户名.github.io/blog/` 的网址，发给任何人都能打开。

**两种方式都不用安装 git、不用敲命令行**，全程鼠标操作。下面第一段是在你这台机器上实测过的结果，不是网上抄的说法。

---

## 先说结论：哪条路走得通

| 托管服务 | 默认网址长相 | 国内是否可访问（本机实测） | 是否永久 | 需要注册 |
| --- | --- | --- | --- | --- |
| **Netlify** | `xxx.netlify.app` | ✅ 可达（538ms） | 认领后永久 | 认领时需要 |
| **GitHub Pages** | `用户名.github.io` | ✅ 可达（1162ms） | 永久 | 需要 |
| Vercel | `xxx.vercel.app` | ❌ 不可达 | — | — |
| Cloudflare Pages | `xxx.pages.dev` | ❌ 不可达 | — | — |

所以下面只讲**前两种**。第三种选择是买域名 + 自己的服务器，等你以后学到后端再说。

---

# 备用方案一：Netlify（不用 GitHub 的最快路径）

好处：不需要先注册，拖一个文件夹上去就有网址了。

### 1. 确认站点已经构建

如果你改过 `posts/` 里的文章，先在 `blog` 目录下跑一次：

```bash
npm run build
```

> 这一步把 Markdown 编译成 `assets/js/posts-data.js`。**这个文件必须一起上传**，否则线上打开是一片空白。

### 2. 拖拽发布

1. 打开 <https://app.netlify.com/drop>
2. 在文件资源管理器里打开 `E:\code\blog`，**点进文件夹内部，按 `Ctrl+A` 全选里面的内容**
3. 把它们拖到网页中间的虚线上传区（拖的是文件夹**里面的东西**，不是 `blog` 文件夹本身）
4. 等几秒，网页会直接给你一个网址，类似 `https://cheerful-cat-8a3f21.netlify.app`
5. 这个网址现在就能打开、就能发给别人了

整个站点只有 0.18 MB，上传基本是瞬间完成。

### 3. 认领站点（重要）

未登录发布出来的站点是"匿名"的。页面上会提示你注册或登录来认领它，**建议当场认领**（用邮箱或 GitHub 账号都行）。认领之后站点才真正归你管，才不会过期，才能改名字。

### 4. 以后更新文章

1. 本地写完文章 → `npm run build`
2. 回到 Netlify，进入这个站点的 **Deploys** 页面
3. 页面底部有一个拖拽区，把 `E:\code\blog` 里的内容重新拖一次
4. 几秒后线上就更新了

---

# 备用方案二：GitHub Pages 网页手工操作

好处：完全免费、永久、可以和 GitHub Actions 配合做到"只上传 Markdown，网站自动重建"。

### 1. 注册 GitHub

打开 <https://github.com/signup>，用邮箱注册（免费）。已有账号就跳过。

### 2. 新建仓库

1. 打开 <https://github.com/new>
2. **Repository name** 填 `blog`
3. 选 **Public**（公开。GitHub Pages 免费版要求仓库公开）
4. 不要勾 "Add a README file"
5. 点 **Create repository**

### 3. 上传博客文件

1. 在新仓库页面点 **uploading an existing file**（或上方 `Add file` → `Upload files`）
2. 打开 `E:\code\blog`，**`Ctrl+A` 全选里面的内容**，拖进网页的上传区
   - ⚠️ 拖的是 `blog` 文件夹**里面的内容**。如果拖文件夹不生效，就先把 `assets`、`posts`、`pages` 这些文件夹逐个拖进去，再拖散装文件
3. 等文件列表全部传完（`posts-data.js` 有 60 多 KB，别中途关页面）
4. 下方 Commit changes 填 `首次发布`，点 **Commit changes**

**上传后对照这份清单检查一遍**（在仓库首页就能看全）：

- [ ] `index.html` 在仓库**最外层**（不是被套在 `blog/` 文件夹里）
- [ ] `assets/js/posts-data.js` 在（漏了它 = 线上白屏）
- [ ] `assets/css/style.css` 在（漏了它 = 样式全丢）
- [ ] `.nojekyll` 在（**最容易漏**。漏了 GitHub 会拿 Jekyll 去解析你的 Markdown，很可能构建失败或页面错乱）
- [ ] `assets/` `posts/` `pages/` 是文件夹，不是被拖平的散文件

### 4. 开启 Pages

1. 进仓库的 **Settings** → 左侧 **Pages**
2. **Source** 选 `Deploy from a branch`
3. **Branch** 选 `main`，目录保持 `/ (root)`，点 **Save**
4. 等 1~2 分钟刷新，顶部出现绿色提示：
   `Your site is live at https://你的用户名.github.io/blog/`

这个网址就是永久的公网地址了。

### 5. 以后更新文章

省事做法：

1. 本地写完 → `npm run build`
2. 仓库页面 → `Add file` → `Upload files`
3. 把**改动过的文件**拖进去（通常就是 `posts/新文章.md` 和 `assets/js/posts-data.js`）
4. Commit，等 1 分钟，线上自动更新

上传同名文件会覆盖，不用先删旧的。

### 6. 进阶：让 GitHub 自动构建（可选）

本地有 `.github/workflows/deploy.yml` 这个工作流文件，但**它目前没有推到线上**——因为 `gh` 的登录凭据里没有 `workflow` 权限，GitHub 不允许这种凭据修改工作流文件（`npm run deploy` 会自动跳过它并提示你）。

想要的话有两条路：

**A. 在网页上手动建（推荐，不用重新授权）**

1. 打开 <https://github.com/Hard-5/blog/new/main/.github/workflows>
2. 文件名填 `deploy.yml`
3. 把本地 `E:\code\blog\.github\workflows\deploy.yml` 的内容整个复制粘贴进去
4. Commit changes

**B. 给凭据补上 workflow 权限**

```bash
E:\code\.tools\bin\gh.exe auth refresh -s workflow
```

然后在浏览器里确认一次，之后 `npm run deploy` 就能正常推送工作流文件了。

启用之后，把 Settings → Pages → **Source** 改成 `GitHub Actions`，以后只要上传新的 `.md`，站点就会自动重建。

> 注意：改成 `GitHub Actions` 之后，第 4 步那种 `Deploy from a branch` 就失效了，两者只能选一个。
> 只是想更新文章的话，其实用 `npm run deploy` 两秒钟就够了，不一定需要这个。

---

## 把网址写回配置（推荐）

知道网址后，打开 `site.json`，把 `url` 填成你的网址：

```json
{
  "url": "https://你的网址/"
}
```

再跑一次 `npm run build`，会额外生成 `feed.xml`（RSS 订阅源），别人可以订阅你的博客。改完后记得重新上传 `site.json` 和 `assets/js/posts-data.js`。

---

## 常见问题

**打开网址是 404？**
GitHub Pages 首次部署要等 2 分钟。超过 5 分钟还 404，检查 Settings → Pages 里分支是不是 `main`、目录是不是 `/ (root)`。

**页面能开但一片空白？**
多半是漏传了 `assets/js/posts-data.js`。去仓库看看这个文件在不在；不在就本地 `npm run build` 再传一次。

**样式全丢了？**
漏传了 `assets/css/style.css`，或者拖文件夹时把目录结构拖平了。GitHub 上应该能看到 `assets/css/style.css` 这样的三层结构。

**为什么不用 Vercel / Cloudflare Pages？**
它们的默认域名（`*.vercel.app`、`*.pages.dev`）在国内打不开——这是我在这台机器上实测的结果。它们本身是好服务，但要配自己的域名才能在国内正常访问，等你以后买了域名再说。

**想用自己的域名？**
Netlify 在 Site settings → Domain management 里加；GitHub Pages 在 Settings → Pages → Custom domain 里填，然后到域名服务商加一条 CNAME 记录指向你的托管地址。

**以后想用命令行自动化？**
装 Git（`winget install Git.Git`），然后：

```bash
cd E:\code\blog
git init
git add .
git commit -m "首次发布"
git branch -M main
git remote add origin https://github.com/你的用户名/blog.git
git push -u origin main
```

第一次 push 会弹浏览器让你授权 GitHub。之后更新只要 `git add . && git commit -m "新增文章" && git push`。
